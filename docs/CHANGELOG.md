[33;1mWARN [0m [1m[90mgit_cliff_core::changelog[39m[0m > [1m[90mprocess_commits[39m[0m[1m[90m: [39m[0m1 commit(s) were skipped due to parse error(s) (run with `-vv` for details)
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Conventional Commits](https://www.conventionalcommits.org).
## [unreleased]

### Bug Fixes

- Reset chapter input fields on modal open
- Allow Cloudflare Web Analytics and Vercel Speed Insights
- Use negative-lookahead pattern for SPA rewrite
- SPA fallback with catch-all rewrite + auto-generated 404.html
- Track loaded note content to prevent false dirty flag
- Harden GitHub Actions workflow security
- Correct CSS loading with Trunk data-trunk directives
- Resolve reactive tracking warnings and update env var message
- Read Supabase config from localStorage with in-app config form
- Add all missing component CSS definitions (~500 lines)
- Provide context states inside Router scope
- Enable chrono wasmbind feature for WASM time support
- Use fixed ISO format for timestamps on WASM
- Use provide_context + mount_to_body closure pattern
- Pass stores as props through Router boundary
- Provide context inside each route view closure
- Use Owner::new_root + OnceLock for signals outside Router
- Initialise stores inside mount_to_body closure
- Enable wasm-opt bulk-memory flags for Rust 1.87+ compatibility
- Disable wasm-opt to unblock Windows build and clean up dead code
- Allow inline bootstrap script and cloudflare beacon in CSP
- Stop chapter-select reactive loop and remove broken stylesheet link
- Use /(.*) rewrite so SPA deep links resolve on Vercel
- Correct check_check icon to match Lucide double-check paths
- Make ProgressBar reactive so book cards show live progress
- Remove cleanUrls so SPA rewrite to /index.html resolves

### Build

- Bump rust-version to 1.97
- Add MIT LICENSE

### CI

- Add TOME CI for lint and build test
- Update trunk build
- Update trunk build --release

### Documentation

- Update README.md
- Refresh README with security, a11y, deployment sections
- Add CONTRIBUTING.md with setup, conventions, and PR guide
- Update AGENTS.md to reflect Rust/Leptos stack
- Update README with docs links, accurate scripts, and license details
- Add SECURITY.md with vulnerability reporting policy
- Point security references to SECURITY.md

### Features

- Migrate from Vue 3 to Leptos 0.8 CSR (Trunk + Vercel)
- Build.rs generates supabase config from env vars

### Miscellaneous Tasks

- Add first commit
- Add biome for lint and format
- Add vercel.json and add csp headers
- Replace lucide-vue-next with @lucide/vue
- Clarify  Lucide integration syntax
- Remove Vue/TypeScript configuration files
- Remove unused reactive_graph direct dependency
- Update vercel.json and CI for production deploy
- Pass all CI steps (check, clippy, fmt, test, build)

### Refactor

- Remove unused ui
- Address comprehensive code audit
- Replace context-based stores with thread_local globals

### Styling

- Reformat codebase with 2-space indent (rustfmt.toml update)

<!-- generated by git-cliff -->
